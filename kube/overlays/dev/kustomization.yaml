apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
- ../../base

namespace: dev

# namePrefix: dev-
commonLabels:
  env: dev

resources:
  - namespace.yaml

patches:
  - replica-count.yaml
  - node-port.yaml

namespace: dev

images:
  - name: cluster-registry:32000/hitcounter
    newTag: "1.0"
